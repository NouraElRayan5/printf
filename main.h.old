#ifndef MAIN_H
#define MAIN_H

#include <stdlib.h>
#include <stdarg.h>
#include <unistd.h>
#include <stdio.h>

/**
 * struct my_struct - match the conversion specifires for printf
 * @c: type char pointer of the specifier
 * @f: tybe pointer to function
*/
typedf struct my_struct
{
char *c;
int (*f)();    
} format;

int _putchar(char c);
int _printf(const char *format, ...);
int print_char(va-list val);
int print_str(va_list val);
int str_length(char *s);
int str_lenc(const char *s);
int print_int(va_list arg);
int _abs(int num);
int print_dec(va_list val);
int print_per(viod);
#endif
